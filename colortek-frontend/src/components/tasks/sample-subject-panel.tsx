"use client";
import { Badge } from "@/components/tailgrids/core/badge"; import { Card, CardDescription, CardTitle } from "@/components/tailgrids/core/card"; import type { SampleSubjectContext } from "@/types/samples"; import Link from "next/link";
export function SampleSubjectPanel({subject}:{subject:SampleSubjectContext}){return(<Card className="mb-4"><CardTitle>Sample requirement</CardTitle>{subject.is_presale?<Badge color="warning" size="sm" className="mb-2">Pre-sale</Badge>:null}<p className="text-sm"><Link href={`/samples/${subject.reference}`} className="text-brand-500">{subject.reference}</Link> · {subject.color}{subject.texture?` · ${subject.texture}`:""}</p>{subject.parent_sample?<CardDescription className="mt-2">Modification of {subject.parent_sample.reference}{subject.parent_sample.rejection_reason?`: "${subject.parent_sample.rejection_reason}"`:""}</CardDescription>:null}{subject.reference_photo?.url?<img src={subject.reference_photo.url} alt="" className="mt-3 max-h-64 rounded border"/>:null}</Card>);}